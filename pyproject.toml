[project]
name = "fluid-analysis-util"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.0",
    "polars>=1.42.1",
    "pyarrow>=25.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
